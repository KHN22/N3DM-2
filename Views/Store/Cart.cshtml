@model List<Marketplace.Models.CartItem>

@{
    ViewData["Title"] = "Your Cart";
}

<div class="container" style="padding-top:28px;">
    <h1>Your Cart</h1>
    @if (Model == null || Model.Count == 0)
    {
        <p>Your cart is empty. <a href="/Store">Continue shopping</a></p>
    }
    else
    {
        <table class="table">
            <thead><tr><th>Item</th><th>Price</th><th>Qty</th><th>Total</th><th></th></tr></thead>
            <tbody>
            @foreach(var item in Model)
            {
                <tr>
                    <td>@item.Title</td>
                    <td>$@item.Price.ToString("F2")</td>
                    <td>@item.Quantity</td>
                    <td>$@((item.Price * item.Quantity).ToString("F2"))</td>
                    <td>
                        <form asp-action="RemoveFromCart" method="post" style="display:inline">
                            <input type="hidden" name="id" value="@item.ProductId" />
                            <button class="btn btn-sm">Remove</button>
                        </form>
                    </td>
                </tr>
            }
            </tbody>
        </table>

        <div style="margin-top:12px;">
            <a class="btn btn-primary" href="@Url.Action("Checkout", "Store")">Proceed to checkout</a>
            <a class="btn" href="/Store">Continue shopping</a>
        </div>
    }
</div>
