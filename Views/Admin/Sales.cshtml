@{
    ViewData["Title"] = "Sales Overview";
    ViewData["ActivePage"] = "Admin";
    ViewData["AdminPage"] = "Sales";
    Layout = "_AdminLayout";
}

<h1>Sales Overview</h1>
<p class="page-desc">Track revenue, transactions, and marketplace performance</p>

@* ── Stats ── *@
<div class="stats-grid">
    <div class="stat-card">
        <span class="stat-label">Total Revenue</span>
        <span class="stat-value">$142,580</span>
        <span class="stat-change positive">+18% vs last year</span>
    </div>
    <div class="stat-card">
        <span class="stat-label">Transactions</span>
        <span class="stat-value">3,412</span>
        <span class="stat-change positive">+9% vs last year</span>
    </div>
    <div class="stat-card">
        <span class="stat-label">Avg. Order Value</span>
        <span class="stat-value">$41.78</span>
        <span class="stat-change positive">+5%</span>
    </div>
    <div class="stat-card">
        <span class="stat-label">Refund Rate</span>
        <span class="stat-value">2.1%</span>
        <span class="stat-change negative">+0.3%</span>
    </div>
</div>

@* ── Revenue Chart ── *@
<div class="card" style="margin-bottom:32px;">
    <div class="card-body">
        <h2 style="font-size:1.125rem; font-weight:600; margin-bottom:16px;">Revenue by Month</h2>
        <div class="bar-chart-mock">
            <div class="bar" style="height:35%;"></div>
            <div class="bar" style="height:42%;"></div>
            <div class="bar" style="height:38%;"></div>
            <div class="bar" style="height:55%;"></div>
            <div class="bar" style="height:48%;"></div>
            <div class="bar" style="height:62%;"></div>
            <div class="bar" style="height:58%;"></div>
            <div class="bar" style="height:75%;"></div>
            <div class="bar" style="height:70%;"></div>
            <div class="bar" style="height:88%;"></div>
            <div class="bar" style="height:92%;"></div>
            <div class="bar" style="height:100%;"></div>
        </div>
        <div style="display:flex; justify-content:space-between; padding:8px 20px 0; font-size:0.75rem; color:var(--text-muted);">
            <span>Jan</span><span>Feb</span><span>Mar</span><span>Apr</span><span>May</span><span>Jun</span>
            <span>Jul</span><span>Aug</span><span>Sep</span><span>Oct</span><span>Nov</span><span>Dec</span>
        </div>
    </div>
</div>

@* ── Top Selling Products ── *@
<div class="card">
    <div class="card-body">
        <h2 style="font-size:1.125rem; font-weight:600; margin-bottom:16px;">Top Selling Products</h2>
        <div class="table-wrapper">
            <table class="data-table">
                <thead>
                    <tr>
                        <th>Product</th>
                        <th>Units Sold</th>
                        <th>Revenue</th>
                    </tr>
                </thead>
                <tbody>
                    @if (ViewBag.TopProducts != null)
                    {
                        foreach (var p in (IEnumerable<dynamic>)ViewBag.TopProducts)
                        {
                            <tr>
                                <td style="color:var(--text-primary); font-weight:500;">@p.Title</td>
                                <td>@p.Quantity</td>
                                <td style="font-weight:600; color:var(--text-primary);">@(((decimal)p.Revenue).ToString("C"))</td>
                            </tr>
                        }
                    }
                    else
                    {
                        <tr><td colspan="3">No sales data available.</td></tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>

<h2 style="margin-top:18px">Recent Orders</h2>
<table class="table">
    <thead><tr><th>Order</th><th>Customer</th><th>Items</th><th>Total</th><th>Date</th></tr></thead>
    <tbody>
    @if (ViewBag.RecentOrders != null)
    {
        foreach (var o in (IEnumerable<dynamic>)ViewBag.RecentOrders)
        {
            <tr>
                <td>@o.OrderId</td>
                <td>@o.CustomerEmail</td>
                <td>@o.ItemCount</td>
                <td>@(((decimal)o.TotalAmount).ToString("C"))</td>
                <td>@(((DateTime)o.CreatedAt).ToString("g"))</td>
            </tr>
        }
    }
    else
    {
        <tr><td colspan="5">No recent orders</td></tr>
    }
    </tbody>
</table>
